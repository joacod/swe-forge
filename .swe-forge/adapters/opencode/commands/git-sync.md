---
description: Return to the remote default branch after a human merge
---

The user explicitly invoked `git-sync`. Read
@~/.config/opencode/swe-forge/.swe-forge/policies/delivery.md and perform only
its `sync` action. Do not perform another delivery action.

Raw action arguments:
$ARGUMENTS
