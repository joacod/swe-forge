---
description: Return to the remote default branch after a human merge
---

The user explicitly invoked the `git-sync` delivery action.

Read @.swe-forge/policies/delivery.md and follow its `sync` action only.
Apply that action to the user-provided arguments below; keep this adapter as a
thin loader and do not perform another delivery action.

Raw action arguments:

$ARGUMENTS
