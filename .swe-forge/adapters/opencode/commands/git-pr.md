---
description: Create or report a pull request for the current pushed branch
---

The user explicitly invoked `git-pr`. Read
@~/.config/opencode/swe-forge/.swe-forge/policies/delivery.md and perform only
its `create_pull_request` action. Pass the raw arguments through unchanged; the
canonical policy recognizes standalone `draft`. Do not perform another delivery
action.

Raw action arguments:
$ARGUMENTS
