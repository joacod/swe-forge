---
description: Push the current reviewed branch without creating a PR
---

The user explicitly invoked `git-push`. Read
@~/.config/opencode/swe-forge/.swe-forge/policies/delivery.md and perform only
its `push` action. Do not perform another delivery action.

Raw action arguments:
$ARGUMENTS
