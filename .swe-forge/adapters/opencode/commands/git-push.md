---
description: Push the current reviewed branch without creating a PR
---

The user explicitly invoked the `git-push` delivery action.

Read @.swe-forge/policies/delivery.md and follow its `push` action only.
Apply that action to the user-provided arguments below; keep this adapter as a
thin loader and do not perform another delivery action.

Raw action arguments:

$ARGUMENTS
