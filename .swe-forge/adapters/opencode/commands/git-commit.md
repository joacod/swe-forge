---
description: Review and commit selected local changes without pushing
---

The user explicitly invoked the `git-commit` delivery action.

Read @.swe-forge/policies/delivery.md and follow its `commit` action only.
Apply that action to the user-provided arguments below; keep this adapter as a
thin loader and do not perform another delivery action.

Raw action arguments:

$ARGUMENTS
