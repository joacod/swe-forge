---
description: Validate and commit selected local changes without pushing
---

The user explicitly invoked `git-commit`. Read
@~/.config/opencode/swe-forge/.swe-forge/policies/delivery.md and perform only
the `commit` action. Do not perform another delivery action.

Raw action arguments:
$ARGUMENTS
