---
description: Validate and commit selected local changes without pushing
argument-hint: "[paths|message]"
---

The user explicitly invoked `/git-commit` through Pi. Read
`~/.pi/agent/swe-forge/.swe-forge/policies/delivery.md` and perform only
`commit`. Do not perform another delivery action.

User-provided action arguments:
$ARGUMENTS
